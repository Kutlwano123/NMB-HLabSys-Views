@using NMB_HLabSys_VIEWS
@model LabTechnicianUser
@{
    ViewData["Title"] = "Delete Lab Technician";
    Layout = "~/Views/Shared/_ManagersLayout.cshtml";
}
<div class="row justify-content-center">
    <div class="col-md-6">

        <div class="card shadow-sm border-danger">
            <div class="card-header bg-danger text-white">
                <h5 class="mb-0">Confirm Deletion</h5>
            </div>
            <div class="card-body">
                <p>Are you sure you want to delete the following lab technician?</p>

                <dl class="row">
                    <dt class="col-sm-4">Name</dt>
                    <dd class="col-sm-8">@Model.Name</dd>
                    <dt class="col-sm-4">Surname</dt>
                    <dd class="col-sm-8">@Model.Surname</dd>
                    <dt class="col-sm-4">Employee Number</dt>
                    <dd class="col-sm-8">@Model.EmployeeNumber</dd>
                    <dt class="col-sm-4">SA ID Number</dt>
                    <dd class="col-sm-8">@Model.SouthAfricanIdNumber</dd>
                </dl>

                <div class="alert alert-warning">
                    <i class="bi bi-exclamation-triangle"></i>
                    This action cannot be undone.
                </div>

                <form asp-action="DeleteLabTechnician" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="LabTechnicianID" value="@Model.Id" />

                    <div class="d-flex justify-content-between">
                        <a asp-controller="LabManager" asp-action="LabTechnicians" class="btn btn-secondary">Cancel</a>
                        <button type="submit" class="btn btn-danger">Yes, Delete</button>
                    </div>
                </form>

            </div>
        </div>

    </div>
</div>
