@model NMB_HLabSys_VIEWS.ViewModels.DeleteTestTypeViewModel
@{
    ViewData["Title"] = "Delete Test Type";
    Layout = "~/Views/Shared/_ManagersLayout.cshtml";
}
<div class="row justify-content-center">
    <div class="col-md-6">

        <div class="card shadow-sm border-danger">
            <div class="card-header bg-danger text-white">
                <h5 class="mb-0">Confirm Deletion</h5>
            </div>
            <div class="card-body">
                <p>Are you sure you want to delete the following test type?</p>

                <dl class="row">
                    <dt class="col-sm-4">Test Type</dt>
                    <dd class="col-sm-8">@Model.TestName</dd>

                    <dt class="col-sm-4">Category</dt>
                    <dd class="col-sm-8">@Model.CategoryName</dd>

                    <dt class="col-sm-4">Sample Type</dt>
                    <dd class="col-sm-8">@Model.SampleTypeName</dd>

                    <dt class="col-sm-4">Turnaround Time</dt>
                    <dd class="col-sm-8">@Model.TurnaroundTimeMinutes minutes</dd>
                </dl>

                <div class="alert alert-warning">
                    <i class="bi bi-exclamation-triangle"></i>
                    This action cannot be undone.
                </div>

                <form asp-action="DeleteTestType" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="SupplierID" value="@Model.Id" />

                    <div class="d-flex justify-content-between">
                        <a asp-controller="LabManager" asp-action="TestTypes" class="btn btn-secondary">Cancel</a>
                        <button type="submit" class="btn btn-danger">Yes, Delete</button>
                    </div>
                </form>

            </div>
        </div>

    </div>
</div>